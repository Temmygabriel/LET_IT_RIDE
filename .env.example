# The base URL of the deployed runner (Deno Deploy) that owns the ride loop.
# The frontend is a thin client that POSTs a config and polls ride state here.
# Set this in Vercel's Environment Variables (and in a local .env for `npm run dev`).
# Example: https://let-it-ride-runner.deno.dev
VITE_RUNNER_URL=
